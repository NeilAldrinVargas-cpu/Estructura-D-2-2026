#include <iostream>

using namespace std;

int main() {
    int numeros[8];
    int suma = 0;

    // 1. Leer los 8 números enteros
    for (int i = 0; i < 8; i++) {
        cin >> numeros[i];
    }

    // 2. Sumar solo los elementos en índices impares (1, 3, 5, 7)
    for (int i = 1; i < 8; i += 2) {
        suma += numeros[i];
    }

    // 3. Mostrar el resultado
    cout << "Suma índices impares = " << suma << endl;

    return 0;
}
